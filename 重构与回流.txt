重构与回流：
浏览器的重构是改变每个元素外观时所触发的浏览器行为，比如颜色，背景样式等发生改变而进行的重新构造新外观的过程，重构不一定会伴随回流

回流是说浏览器为了重新渲染页面而重新刷新页面，计算这个元素的大小位置属性等，比如元素尺寸，内容，位置的改变，也会引起回流

所以避免回流的办法： 改变或者添加css样式，直接通过添加类的方式，不要直接在DOM上设置，并且操作某一块元素的时候，可以让其脱离标准来，这样就不会引起回流了
