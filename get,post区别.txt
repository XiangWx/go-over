1，get，post 都是一种传递数据的方式，本质一样，都是发送请求接受结果
2.，get 是从服务器上获取数据，post是向服务器传送数据
3，从安全角度看，get，post都一样，但是get是通过URL地址栏上提交数据，而post是把数据放在请求体中提交
4，从幂等性角度看，get更安全（请求发送给服务器后，不论发送多少次请求得到的结构始终是相同的）
5，传送数据的文件大小，post更大
6，用post必须要用到FORM表单